package interfaccia;

import javax.swing.*;
import java.awt.*;

public class Tabellone extends JPanel {
    public Tabellone(String casa, String ospiti, String dateEvent){
        setLayout(new GridLayout(3,3));
        //todo layout team A, tempo gioco, Team B \n pt A, data match, pt B, time out A, primo/secondo tempo, time out B
        /*add(),add(),add();
        add(),add(),add();
        add(),add(),add()
        */
    }

    public class squadra extends JLabel{
        public squadra (){

        }
    }
}
